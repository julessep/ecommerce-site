extends index.pug

block content
  h1 Your Payments!
  ul
    each payment in payments
      -let url = `http://localhost:5000/payments/` + payment.id
      a(href=url) #{payment.cardNickname} 
      br
  form(action="/postPayment")
    button(class="btn btn-outline-secondary" type="submit") Add New Payment